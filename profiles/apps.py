"""Configuration de l'application profiles."""
from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Configuration de l'application profiles.

    Cette classe définit la configuration pour l'application Django profiles,
    qui gère les profils utilisateurs et leurs informations personnelles.
    """
    name = 'profiles'
