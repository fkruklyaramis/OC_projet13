"""Configuration de l'application lettings."""
from django.apps import AppConfig


class LettingsConfig(AppConfig):
    """
    Configuration de l'application lettings.

    Cette classe définit la configuration pour l'application Django lettings,
    qui gère les locations immobilières et leurs adresses associées.
    """
    name = 'lettings'
